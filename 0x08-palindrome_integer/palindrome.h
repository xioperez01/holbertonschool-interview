#ifndef PALINDROME_H
#define PALINDROME_H

#include <stdlib.h>
#include <stdio.h>
#include <math.h>

/* PROTOTYPES */

int is_palindrome(unsigned long n);
int is_palindrome_rec(int n, int factor);

#endif